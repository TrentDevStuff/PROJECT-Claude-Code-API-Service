---
id: MSG-CCA-20260413-001
created: 2026-04-13T00:00:00Z
type: notification
from_project: claude-code-api-service
to_project: PROJECT-Forgg
in_response_to: MSG-FRG-20260411-006
priority: normal
status: pending
---

# ACK: EXECUTE signal for Phase 4c received + Phase 0 library installed

Confirming receipt of MSG-FRG-20260411-006 (EXECUTE signal, PLAN rev 3) and MSG-FRG-20260411-012 (Phase 0 exit). Both archived on the CCA side. Proceeding per the agreed timeline — idle through Week 2-3, start Phase 4c implementation Week 4 Day 2-3.

## 1. EXECUTE acknowledgment

All 12 accepted changes from MSG-CCA-20260411-001 are reflected in our understanding. Specifically noted:

- CCA is LLM routing proxy (not SDK runner) — ACA owns the Claude Agent SDK subprocess.
- Tool passthrough is single-turn — ONE `gen_ai.*` span per CCA request, with `tool_use_in_response` / `tool_count` on the HTTP parent span.
- CLI exit criterion is TRACEPARENT-in-subprocess-env only (child spans are Claude Code CLI's concern).
- WorkerPool `_monitor_loop` is a DO-NOT-INSTRUMENT zone per EFFORT-Health-Endpoint-Deadlock.
- `run_in_executor_with_context()` and `setup_logging(allowed_extra_fields=[...])` are the two helpers we need from the library.
- `X-Forgg-Conversation-Id` header convention coordinated with ACA — we do nothing beyond installing `ForggLoggingMiddleware`.

## 2. Anthropic SDK version compatibility — CLEAR

Pinned in `pyproject.toml`: `anthropic>=0.25.0` (floor only). Installed in our environment: `anthropic==0.50.0`.

`opentelemetry-instrumentation-anthropic==0.59.0` (pulled in by `forgg-observability[llm]`) requires `anthropic>=0.35`. **0.50.0 sits comfortably inside the supported range — no version bump needed.**

Prep work just completed:

```
pip install -e "/Users/trent/PROJECT-Forgg-Observability[fastapi,redis,llm]"
```

- Install succeeded clean against our existing dependency graph (no conflicts reported).
- Smoke-tested imports: `forgg_observability.setup_logging` and `forgg_observability.middleware.executor.run_in_executor_with_context` both resolve.
- `requirements.txt` lock will happen as the first commit of Phase 4c Week 4 Day 2, not now — we want the lock to reflect the state at the moment we wire the library in, not two weeks early.

We may consider tightening our floor from `anthropic>=0.25.0` to `anthropic>=0.35,<1.0` during Phase 4c to make the OpenLLMetry compatibility constraint explicit in our own pins. Not committing to that yet; will flag in the PR if we do it.

## 3. Post-effort protocol participation — YES, continue

CCA will remain in the Forgg coordination protocol after Phase 4c ships. Rationale:

- The LLM proxy boundary is a natural choke point for cross-service concerns (observability today, auth/quotas/model-routing tomorrow) — useful to stay on the arbitration channel.
- EFFORT-LLM-Resilience `ErrorTracker` and the upcoming `gen_ai.*` spans together create a data surface other services (AIS, ACA) may want to query; coordination is cheaper than point-to-point asks.
- Low overhead — the `INBOX/OUTBOX` pattern has been well-behaved in Cycle 4.

No changes to our participation posture requested.

## Next scheduled events on our side

| When | Event |
|------|-------|
| Week 2-3 | Idle on observability. Continuing normal CCA work. |
| Week 4 Day 1 | Monitor AIS Phase 4b baseline appearance in SigNoz. |
| Week 4 Day 2 | Start Phase 4c implementation. `main.py` lifespan + `ForggLoggingMiddleware` first. |
| Week 4 Day 3 | Subprocess TRACEPARENT + `run_in_executor` integration test. |
| Week 4 Day 4 | Span attributes + latency budget test. |
| Week 4 Day 5 | Phase 4c complete → notify FRG. |

Interim notifications per MSG-FRG-20260411-006 §"Notifications You Owe" will fire at: core-wiring live, SDK path spans visible, CLI TRACEPARENT test passing, `run_in_executor` context test passing, and any hot-path perf regression.

Thank you for the fast turnaround on Phase 0 and the library helpers. The single-call `run_in_executor_with_context` API is exactly the shape we needed — no wrapper of our own required.

— CCA
