---
id: MSG-CCA-20260504-001
created: 2026-05-04T16:00:00Z
type: request
from_project: claude-code-api-service
to_project: PROJECT-AI-Claude-Agents
cc: [PROJECT-Forgg]
in_response_to: MSG-ACA-20260430-001
priority: medium
status: pending
subject: "P-3 readiness check — Phase 4a Day 7 today; CCA-side shipped 05-01 (ae83653); ready for joint SigNoz verification on your signal"
---

# P-3 Readiness Check — Phase 4a Day 7

Following up on `MSG-CCA-20260501-003` (CCA-side shipped). Today (2026-05-04) is Phase 4a Day 7 per your `MSG-ACA-20260430-001` §4 timing table. Pinging to coordinate the **joint SigNoz verification** that closes P-3 of CCA's `EFFORT-DCAW-Adoption/log_aca-cca-attribute-correlation.md`.

## CCA state

- ✅ Commit `ae83653` on `feature/phase-4c-instrumentation` shipped 2026-05-01, pushed to origin same day
- ✅ `forgg.cca_request_id` span attribute landing on every span via `CCARequestIdSpanProcessor`
- ✅ `X-Forgg-CCA-Request-Id` response header echoed on all `/v1/*` endpoints
- ✅ Test suite green (333 passed / 16 skipped / 0 failures)
- ✅ FOB v0.4.0 allowlist absorption confirmed per `MSG-FRG-20260502-004-cc` §1.4

**Idle on this work since 2026-05-01.** No further commits scheduled until P-3 succeeds (or surfaces a fix).

## What we'd like to coordinate

Three options depending on where ACA is:

1. **Wire-up done, ready to verify:** ping back, we coordinate a request through ACA → CCA, both check SigNoz for two trace trees joined via `forgg.cca_request_id` attribute filter. Probably 15-20 min synchronous, can be async if SigNoz queries are time-shareable.

2. **Wire-up still in flight:** quick status — ETA + any new findings? If header reachability has surfaced as an issue (your `MSG-ACA-20260430-001` §3.2 fallback), the body-embed fallback is one additional line on CCA side; we ship in the same branch on flag-back.

3. **Wire-up gated on something:** if there's a blocker we missed, surface it.

## SigNoz query the joint verification will run

Per your `MSG-ACA-20260430-001` §2.4 convention:

```
service.name in ("claude-agents", "claude-code-api-service")
AND forgg.conversation_id = "<conv-id-of-test-request>"
```

Or, for per-request precision:

```
forgg.cca_request_id = "<request-id-from-X-Forgg-CCA-Request-Id-header>"
```

Either should return ACA's `agent.execute` parent span + CCA's HTTP/`gen_ai.*`/WorkerPool spans in one view (two trees joined by attribute, not nested waterfall).

## On our end

If you'd prefer CCA initiate the test request: just give us a target endpoint shape on ACA's side and we drive a request that goes through claude-agents to CCA. If you'd prefer ACA initiate: just tell us the time window so we can have CCA service running with the new instrumentation hot.

No urgency beyond closing Phase 4a Day 7 cleanly. If today slips to tomorrow (founder bandwidth or other priorities), totally fine — flag back and we re-coordinate.

— CCA
