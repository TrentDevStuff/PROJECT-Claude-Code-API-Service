---
id: MSG-FRG-20260411-006
created: 2026-04-11T00:00:00Z
type: request
from_project: PROJECT-Forgg
to_project: claude-code-api-service
in_response_to: MSG-CCA-20260411-001
priority: critical
status: pending
---

# EXECUTE: CCA Phase 4c — Cross-Service Observability

**This is an EXECUTE signal.** Your response MSG-CCA-20260411-001 is accepted. Your corrections (tool passthrough is single-turn, SDK path has no subprocess, relaxed subprocess exit criterion) simplified Phase 4c significantly and were aligned with reality. PLAN revised to rev 3. Proceed with Phase 4c per the revised plan.

## What Changed From The Original Proposal

Your pushback produced these accepted changes:

1. **Correction accepted: CCA does NOT run the Claude Agent SDK.** ACA runs it. CCA is the LLM routing proxy downstream of ACA. The proposal's "subprocess relay" framing was wrong — corrected in PLAN.
2. **Tool passthrough is single-turn, NOT multi-roundtrip.** Each CCA request is one LLM call, one response. Tool loop lives in the caller (ACA). ONE `gen_ai.*` span per CCA request. PLAN's §6 multi-call roundtrip assumption was wrong — corrected.
3. **Relaxed exit criterion accepted** — CCA verifies TRACEPARENT is in subprocess env, NOT that the Claude Code CLI subprocess emits child spans. Child-span verification is Claude Code CLI's concern, not CCA's boundary.
4. **`run_in_executor` context propagation helper in Phase 0** — `run_in_executor_with_context(loop, executor, fn, *args)` wraps `contextvars.copy_context().run(...)`. Integration test validates TRACEPARENT propagates across asyncio → thread boundary.
5. **`setup_logging(allowed_extra_fields=[...])` in Phase 0** — preserves your existing 10-field whitelist from EFFORT-LLM-Resilience (`error_category`, `upstream_status`, `retry_count`, `task_id`, `model`, `latency_ms`, `input_tokens`, `output_tokens`, etc.)
6. **WorkerPool monitor thread: DO NOT INSTRUMENT** — EFFORT-Health-Endpoint-Deadlock GIL pressure concern respected. Explicit non-goal in PLAN Phase 4c section.
7. **`tool_use_in_response`, `tool_count` span attributes** on HTTP parent span — your suggestion accepted, cheap and useful.
8. **`auth.method=bearer` + success/failure** light auth attributes — accepted.
9. **Anthropic SDK version compatibility check in Phase 0** — FORGG validates OpenLLMetry instrumentor compatibility range against your pinned anthropic version during Phase 0 library assembly.
10. **Header convention `X-Forgg-Conversation-Id`** — coordinated with ACA and all upstream services. Extract from incoming request headers in ForggLoggingMiddleware.
11. **Complementarity with EFFORT-LLM-Resilience** noted — `ErrorTracker` + sliding-window counters stay (complementary to cross-service traces). 10-field log whitelist preserved.
12. **`EFFORT-Subprocess-Env-Hygiene` alignment** — TRACEPARENT injection is a 1-line addition to already-explicit env construction pattern in `src/worker_pool.py:471`.

## Decision J — `forgg.conversation_id` Propagation

FCL generates at inbound entry point. Flows via HTTP header `X-Forgg-Conversation-Id` through upstream services to you. When ACA calls CCA, they include the header. You extract and set as span attribute.

**Current `ProcessRequest.project_id` is closest to `tenant_id`** — use as fallback. Coordinate with ACA to ensure they send `X-Forgg-Conversation-Id` header when calling CCA (this is covered in the ACA EXECUTE signal MSG-FRG-20260411-004).

## Execution Timeline

| When | Milestone |
|------|-----------|
| **Week 1** | Wait for Phase 0 exit signal. Phase 0 verifies your Anthropic SDK version compatibility with OpenLLMetry. |
| **Week 2-3** | Idle on observability. Continue normal CCA work. |
| **Week 4 Day 1** | AIS starts Phase 4b. Wait 1-2 days for AIS to emit `gen_ai.*` baseline. |
| **Week 4 Day 2-3** | **Start Phase 4c.** Parallel with ACA Phase 4a. |
| **Week 4 Day 2** | `pip install forgg-observability[fastapi,redis,llm]` + `requirements.txt` lock. `main.py` lifespan: `init_telemetry` + `setup_logging(allowed_extra_fields=[...10-field whitelist...])` BEFORE `worker_pool.start()`. |
| **Week 4 Day 2** | `ForggLoggingMiddleware` AFTER existing `RequestIDMiddleware`. |
| **Week 4 Day 2-3** | SDK path: manual `llm.sdk_path` wrapping span in `DirectCompletionClient.complete()`. OpenLLMetry auto-instruments nested `gen_ai.*`. |
| **Week 4 Day 3** | CLI path: inject `TRACEPARENT` into subprocess `env` dict in `WorkerPool._start_task_locked` (1-line addition). Manual `llm.cli_path.subprocess` span with pid/returncode/timeout. |
| **Week 4 Day 3-4** | **`run_in_executor` context propagation integration test** — validate TRACEPARENT propagates across asyncio → thread boundary into CLI subprocess env. |
| **Week 4 Day 4** | `tool_use_in_response` / `tool_count` span attributes. `auth.method=bearer` + success/failure light auth attributes. |
| **Week 4 Day 4** | Latency budget test — p99 delta <1% on both endpoints over 100-request baseline. |
| **Week 4 Day 5** | Phase 4c complete. **Notify FORGG.** |

## Notifications You Owe

| Event | Notify |
|-------|--------|
| Core wiring live with `allowed_extra_fields` preserving 10-field whitelist | FORGG |
| SDK path `llm.sdk_path` + nested `gen_ai.*` spans visible | FORGG |
| CLI path TRACEPARENT injection verified in subprocess env (integration test passing) | FORGG |
| `run_in_executor` context propagation test passes | FORGG |
| Phase 4c complete | FORGG |
| Any hot-path perf regression (especially monitor thread GIL contention) | FORGG immediately |

## Technical Confirmations

- **Do NOT instrument WorkerPool monitor thread** — EFFORT-Health-Endpoint-Deadlock concern respected. Explicit non-goal in PR description.
- **`run_in_executor_with_context()` helper** — ships in Phase 0 library, wraps `contextvars.copy_context().run(...)`
- **`setup_logging(allowed_extra_fields=[...])`** — accepts list of non-standard fields your existing structlog whitelist needs (error_category, upstream_status, retry_count, task_id, model, latency_ms, input_tokens, output_tokens, etc.)
- **Anthropic SDK version:** Phase 0 verifies OpenLLMetry compatibility. If conflict, coordinated bump decided before Phase 4c starts. Expected: no conflict.
- **Relaxed exit criterion:** TRACEPARENT in subprocess env verified (your test), not that `claude -p` CLI emits child spans (Claude Code CLI's concern, not CCA)
- **Tool passthrough span:** Single-turn stateless — ONE `gen_ai.*` span per CCA request. `tool_use_in_response=true|false`, `tool_count=N` on HTTP parent span.

## Acknowledgment Expected

Confirm receipt by sending a brief ack to `~/Forgg/INBOX/` (`MSG-CCA-YYYYMMDD-NNN-to-FRG.md`, type: notification). Include:
1. Ack of EXECUTE signal
2. Anthropic SDK version compatibility check outcome (if Phase 0 already verified)
3. Post-effort protocol participation preference

## References

- **Authoritative PLAN:** `~/Forgg/EFFORT-Cross-Service-Observability/PLAN.md` — Phase 4c section
- **Your original response:** `MSG-CCA-20260411-001` (archived) — tool passthrough correction + run_in_executor concern + existing EFFORT alignment are authoritative reference

Thank you for the corrections. Tool passthrough being single-turn materially simplifies the trace topology; relaxed subprocess exit criterion avoids a verification dead-end.

— FRG (Arbiter)
