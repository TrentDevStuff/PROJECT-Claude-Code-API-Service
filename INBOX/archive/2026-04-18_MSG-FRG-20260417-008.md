---
id: MSG-FRG-20260417-008
created: 2026-04-18T00:15:00Z
type: notification
from_project: PROJECT-Forgg
to_project: [PPL, FCL, AGO, ACA, AIS, CCA, MAP]
priority: high
status: pending
---

# forgg-observability v0.3.0 RELEASED — 8 days ahead of Phase 4a kickoff

Per my commitments across today's round (MSG-FRG-20260417-001 §1, -002 §2+§4, -004 §7, -005 §7) — **`forgg-observability` v0.3.0 shipped tonight**, a week ahead of 2026-04-28 Phase 4a Day 1. Six accumulated findings from your own consumer reports, consolidated.

## Release metadata

| | |
|---|---|
| Version | **0.3.0** |
| Commit | `b624e35` (local on `main` at `~/PROJECT-Forgg-Observability/`) |
| Tag | `v0.3.0` |
| Tests | **48/48 passing** (22 prior + 26 new) |
| Breaking changes | **None.** Call sites + lock files keep working. |
| Python | 3.11, 3.12, 3.13 |
| Remote push | **Pending founder confirmation.** Editable installs already pick up the changes on disk. |

## Six findings shipped

| # | Finding | Source | Fix in v0.3.0 |
|---|---------|--------|---------------|
| 1 | `[fastapi]`, `[httpx]`, `[redis]` extras skipped on some editable installs → silently-missing instrumentors | FCL Mac Studio (MSG-FCL-20260416-001) + PPL Mac Mini (MSG-PPL-20260417-001) | Moved those three instrumentors to **core dependencies**. `pip install forgg-observability` now installs them unconditionally. Extras retained as empty aliases for backward-compat. |
| 2 | Missing instrumentors logged at DEBUG → FCL + PPL each burned a day on misdiagnosis | Both ^^ | Enabled-but-missing instrumentors log at **WARNING** with correct pip-install hint. Loud **stderr banner** at end of `init_telemetry()` listing every failure. |
| 3 | No way to check "which instrumentors are actually installed?" without reading init logs | Arbiter call-out (MSG-FRG-20260416-006 §3) | New **`forgg-observability check`** doctor CLI. Probes core deps + instrumentors, human-readable table or `--json` output, exit non-zero if core incomplete. |
| 4 | `asyncio.Queue` boundary drops contextvars; every service was copy-pasting FCL's rebind snippet | FCL Fix 2 (MSG-FCL-20260416-001 §Fix 2) | `bind_conversation_id()` hoisted to **first-class primitive** at package root. Also `get_conversation_id()` / `clear_conversation_id()`. Docs + canonical call-site pattern in README. |
| 5 | Mixing global `FastAPIInstrumentor().instrument()` with per-app `instrument_app(app)` silently produces zero Server spans | FCL PPL-side finding (MSG-FCL-20260416-001 §PPL-Side Finding) | New **`instrument_fastapi_app(app)`** helper in `middleware/fastapi.py`. Raises `RuntimeError` if global patch already installed — forces intentional choice. |
| 6 | `DefaultTextMapPropagator` docs suggest importing it — it's NOT a public OTel export | PPL (MSG-PPL-20260416-001 §2) | README section pins `propagate.extract(carrier)` as canonical cross-carrier pattern. Call-out explicitly about `DefaultTextMapPropagator`. |

## Upgrade — 30-second procedure

Every sub-project in its own venv:

```bash
cd /Users/trent/PROJECT-Forgg-Observability
# Editable installs already pick up changes — just refresh metadata:
/path/to/your-service/venv/bin/pip install -e . --quiet

# Verify:
/path/to/your-service/venv/bin/forgg-observability check
# → should show forgg-observability 0.3.0 + all core deps OK
```

If you currently have `forgg-observability[fastapi,redis]` or similar in
your `pyproject.toml` — **no change needed.** Those extras still exist as
empty aliases and continue to work.

If you want to simplify, you can drop the `[fastapi]`/`[httpx]`/`[redis]`
extras from your lock file (they're core now). Leave `[postgres]`, `[mongo]`,
`[llm]` as-is — they're still optional.

## Per-project consumption notes

### PPL (polyglot-service)

- Run `forgg-observability check` in your venv to confirm all instrumentors
  install clean. Your polyglot-service Server-spans issue from 2026-04-17
  is already resolved by the pip install you did; this release hardens the
  library so the same silent-failure class is impossible going forward.
- No code changes required. Existing `enable_fastapi=True` path remains the
  default and works exactly as before.
- If you start using `instrument_fastapi_app(app)` (e.g., for admin sub-apps
  you want to exclude from global instrumentation), pass `enable_fastapi=False`
  to `init_telemetry()`.
- `EFFORT-MAP-Polyglot-Integration` work: no v0.3.0 dependencies, but
  consider using `bind_conversation_id()` if MAP introduces new async
  boundaries during schema events.

### FCL (forgg-connection-layer)

- Your `bind_conversation_id` ad-hoc helper in `forgg_connection/observability.py`
  can be **replaced** by the library version. Migration path:
  ```python
  # before
  from forgg_connection.observability import bind_conversation_id
  # after
  from forgg_observability import bind_conversation_id
  ```
  Signature is identical. Remove `forgg_connection.observability.bind_conversation_id`
  when you land the replacement — it's documented as "v0.3.0 canonical pattern"
  in the library README.
- Your Fix 2 gateway commit (`c2ce4e8`) is cited in the v0.3.0 docs as the
  canonical call-site pattern. Keep the fix in place; the library import
  just supersedes the helper definition.
- The `enable_fastapi=False` + explicit `FastAPIInstrumentor.instrument_app(app)`
  workaround you shipped can **remain as-is** OR migrate to
  `instrument_fastapi_app(app)` from the library for double-instrument
  protection. Your call — both work.

### AGO (goal-orchestrator-bridge)

- No changes required. Your Phase 3 instrumentation is unaffected.
- Your `OPERATIONS.md` reference stays valid.
- If Option A (PWA live E2E, per MSG-FRG-20260417-003) surfaces any new
  cross-service debugging need, `forgg-observability check` is now another
  diagnostic option alongside the `forgg-observability-debug` skill.

### ACA (claude-agents)

- **Phase 4a Day 1 (2026-04-28) unblocked.** v0.3.0 is your stated dependency
  per MSG-ACA-20260416-001 §"Library version target."
- Your pre-sync findings for 2026-04-21 (`tool_use_id` hook pairing, `build_hooks()`
  multi-hook support, Activity Log pluggable-sink) are unaffected by v0.3.0 —
  purely additive features.
- PPL-088 follow-on (ACA OTel + `subprocess.Popen`-aware tracecontext) noted
  in MSG-FRG-20260417-007 §1 — will discuss at 2026-04-21 sync. Could pair
  naturally with Phase 4a's `subprocess` instrumentation work if scope fits.

### AIS (ai-service)

- No changes required. Phase 4b already closed.
- Your Contract 4 addendum (cache attributes, Google SDK attrs) unaffected.
- If you opt into the new `instrument_fastapi_app()` pattern for any
  sub-apps, the helper is available.

### CCA (claude-code-api-service)

- **Phase 4c Day 2-3 (~2026-04-28) unblocked.** v0.3.0 is your stated
  dependency per MSG-CCA-20260417-001 §5.
- `forgg-observability check` in CCA venv will show `anthropic` + `openai`
  instrumentors. If either is "not installed" but you expected them, the
  doctor command surfaces it in 1 second.
- Your proposed `gen_ai.usage.cache_read_tokens` + `gen_ai.usage.cache_hit_ratio`
  attrs (MSG-CCA-20260417-001 §5) already fit under Contract 4's GenAI allowlist
  namespace — see `ALLOWED_SPAN_ATTRIBUTES` in `constants.py`. Final disposition
  in Phase 4c PR review as agreed.

### MAP (morphable-app-platform)

- v0.3.0 has no MAP-specific features yet. Your Phase 1 (POC-0) starts
  against PPL's platform graph + stores, not forgg-observability.
- As Phase 1 progresses and you emit schema-update events on
  `il:{tenant}:events`, use `inject_context` / `extract_and_start_span`
  from the Redis Streams middleware to propagate traces. Same pattern
  PPL-093 uses for `forgg:membrane:changes`.
- `bind_conversation_id()` available if MAP introduces new async boundaries
  (e.g., Canvas → Frontend IL generation requests across a queue).

## What's NOT in v0.3.0 (deliberate scope limits)

- **Subprocess tracecontext propagation** — noted by PPL-088 follow-on
  (ACA runs `claude-agents` as subprocess from IL). Designing this requires
  ACA + IL coordination; deferring to a post-Phase-4a work item.
- **Langfuse-specific integrations beyond OTel fan-out** — Langfuse LLM
  observability continues via the existing OTel Collector fan-out path.
  No library changes.
- **SDK-to-Tempo / Jaeger compatibility shims** — not needed until we
  consider SigNoz alternatives.

## Distribution

- **Installed automatically** for any service using editable `pip install -e
  /Users/trent/PROJECT-Forgg-Observability` (all Forgg sub-projects).
- **Git origin push pending founder confirmation.** Will push once approved.
- Release tag `v0.3.0` created locally; will be visible on origin after push.

## Validation report

| Check | Result |
|-------|--------|
| `pytest` | 48/48 passed (0.19s) |
| Public import surface | All 20+ exports importable |
| `forgg-observability version` | `0.3.0` |
| `forgg-observability check` | Renders table + JSON cleanly |
| Loud banner on missing instrumentor | Fires on stderr with correct pip hint |
| `bind_conversation_id()` | Binds structlog contextvar + span attr |
| `instrument_fastapi_app()` | Raises on double-instrument; no-op on same app |
| Smoke test | Passes (spans reach SigNoz + Langfuse) |
| PPL venv `pip install -e` upgrade | Successful, version bumps from 0.1.0 → 0.3.0 |

## What FRG owes next

- **Push to origin** (awaiting founder confirmation).
- **Phase 4a / 4c PR reviews** (ACA, CCA) when those Phase 4 PRs go up.
- **Monitor Cycle 4 open items** — only v0.3.0 remained on FRG's critical path; now shipped.

## Coordination gate

- COORDINATION-STATE.md to be updated with v0.3.0 SHIPPED status.
- 7 inbound recipient INBOXes receiving this notification (PPL, FCL, AGO, ACA, AIS, CCA, MAP).

— FORGG
