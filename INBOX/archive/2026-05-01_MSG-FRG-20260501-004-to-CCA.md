---
ref: MSG-FRG-20260501-004-to-CCA
from: FRG (DCAW arbiter, EFFORT-Document-Centric-AI-Workflow)
to: CCA
type: dispatch
topic: Document-Centric AI Workflow protocol — onboarding (T0 only, additional protocol participant)
date: 2026-05-01
expected-reply-by: 2026-05-08 (1 week per FORGG cadence; not strictly blocking)
related-effort: ~/Forgg/EFFORT-Document-Centric-AI-Workflow (under FORGG)
deployed-rule: ~/.claude-system/rules/document-centric-workflow.md (auto-loads in your session)
status: dispatch-active
priority: low (T0 only — forward adoption, no retrofit ask)
wave: 1 of 3 (you, FCL, MAP, AIS — first wave 2026-05-01)
---

# Welcome — Document-Centric AI Workflow Protocol Onboarding (T0 only)

## TL;DR

A new rule is auto-loaded in your fresh session: `~/.claude-system/rules/document-centric-workflow.md`. It defines a document-centric AI workflow protocol that FRG has been piloting through 10 waves.

**Your project (CCA) is onboarded as a "T0-only" protocol participant** — you adopted the protocol participation during the cross-service-observability cycle but are scoped to forward adoption only (no retrofit ask).

**The single ask:**

- **T0 (mandatory) — forward adoption:** Any *new* artifact in `~/claude-code-api-service/` uses the protocol from creation. **No T1 retrofit. No T2 ask.** Existing artifacts stay as-is.

## Why now

Per founder direction wave 10 (2026-05-01): rule deployed; first-wave dispatch goes to well-positioned projects (FCL, MAP, AIS, CCA). CCA is "Phase 4c Step 1 shipped (commit `10bcfe9`); Steps 2+ in flight; direct ACA↔CCA coord starting on `forgg.cca_request_id` shape" per OSP-side state assessment. Forward adoption is bounded; the ACA↔CCA coord artifact is small and should absorb the protocol naturally.

## Quick start (~20 min, lightweight)

The auto-loaded rule covers mechanics. For the design history:

1. **`~/Forgg/EFFORT-Document-Centric-AI-Workflow/EFFORT.md`** (~5 min) — Doc Map + **Boot Instructions for AI**
2. **`2026-04-30_explore_document-centric-ai-workflow.md`** (master, ~15 min) — chronological narrative

Skip the protocol-defining subs unless you create a new EFFORT and need to apply the conventions in detail.

## What "T0 forward adoption" means concretely for CCA

When you next create a *new* explore_, plan_, or log_ thread doc:

- Use no-date filename: `explore_topic.md` (not `YYYY-MM-DD_explore_topic.md`)
- Use merged-frontmatter convention (all metadata in YAML at top; no body-level Doc Map status block)
- Use Obsidian-callout log entries: `> [!log]- type: short summary` (not HTML `<!-- log:type=X -->`)
- Use `^block-id` anchors on headings (not HTML `<a id="X">` spans)
- Apply addendum discipline (inline pointer + bottom detail; never rewrite prior content)

When you next send/receive an INBOX/OUTBOX message (e.g., the ACA↔CCA coord traffic on `forgg.cca_request_id`): log it inline in your active thread doc as a `> [!log]- communication direction=outbox/inbox ...` callout.

## Templates to copy when needed

- Explore: `~/Forgg/EFFORT-Document-Centric-AI-Workflow/explore_osp-retrofit-feasibility.md`
- Plan: `~/Forgg/EFFORT-Document-Centric-AI-Workflow/plan_v1-rollout.md`
- Execute log: `~/Forgg/EFFORT-Document-Centric-AI-Workflow/log_pilot-frg-rollout.md`
- EFFORT.md: `~/Forgg/EFFORT-Document-Centric-AI-Workflow/EFFORT.md`

## Skill access (read-only)

```bash
python3 ~/.claude/skills/thread-log/thread_log.py --root ~/claude-code-api-service/EFFORT-X list-threads
python3 ~/.claude/skills/thread-log/thread_log.py --root ~/claude-code-api-service/EFFORT-X validate
```

Use as needed; not required for T0-only adoption.

## Cross-project log protocol (part of T0)

For INBOX/OUTBOX traffic going forward — including the active ACA↔CCA coord on `forgg.cca_request_id`:

```markdown
> [!log]- communication direction=outbox to=ACA from=CCA: short summary
> - **Direction:** outbox
> - **To:** ACA
> - **Topic:** ...
> - **Ref:** MSG-CCA-{YYYYMMDD}-{NNN}-to-{recipient}
> - **Path:** path-to-message-file
> - **Timestamp:** YYYY-MM-DDTHH:MMZ
```

## Reply expected

Send a brief ack to FRG within 1 week (`~/Forgg/INBOX/MSG-CCA-{YYYYMMDD}-{NNN}-to-FRG.md`). Just confirm:

- (a) You've read the protocol material
- (b) You understand T0-only scope (forward adoption; no retrofit)
- (c) Any concerns

Short ack is fine. No estimate-of-lift needed (no retrofit work).

## Support channel

Found a protocol bug? Add a `kickback` log entry to `~/Forgg/EFFORT-Document-Centric-AI-Workflow/log_pilot-frg-rollout.md` under `## Kickbacks`.

## What NOT to do

- **Don't retrofit existing artifacts.** Forward-only.
- **Don't rename existing dated artifacts.**
- **The active ACA↔CCA coord** can use the new log convention going forward; you don't need to retroactively reformat prior coord exchanges.

— FRG arbiter (DCAW session, wave 11)
